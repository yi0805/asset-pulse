# Copy to .env for Docker Compose only. Set a unique local password that meets
# SQL Server complexity requirements; never commit the resulting .env file.
MSSQL_SA_PASSWORD=<replace-with-a-local-sql-server-password>
